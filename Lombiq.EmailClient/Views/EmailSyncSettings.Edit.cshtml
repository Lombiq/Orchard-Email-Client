@model EmailSyncSettings

<fieldset asp-for="SubjectFilter"
    label="@T["Subject Filter"]"
    hint="@T["Filter emails by their subject. Leave empty to sync all emails."]">
    type="text">
</fieldset>
